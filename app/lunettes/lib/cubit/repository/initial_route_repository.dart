import 'package:lunettes/constants/app_const.dart';
import 'package:http/http.dart' as http;
import 'dart:convert' as convert;

class InitialRouteRepository {
  // functions to call/check if the user is
  // already exists, logged in and delete the user ID from storage
  Future<bool> sampleRepo(bool isLoggedIn) async {
    // deleteID();
    return true;
  }
}
